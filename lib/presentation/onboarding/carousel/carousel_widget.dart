import 'package:flutter/cupertino.dart';

class CarouselWidget extends StatelessWidget {
  const CarouselWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO(Filippo): next steps
    // returns a BottomSheetWrapContainer()
    // scrolls through a PageView.builder
    // by means of aPageController()

    // review AppNavigator.navigatePage() in intro widget
    // review custom popup rout, custom route and app navigator
    // review app scaffold and connectivity widget
    return Text('TBC');
  }
}
